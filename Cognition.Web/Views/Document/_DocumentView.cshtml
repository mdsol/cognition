@model Cognition.Shared.Documents.Document
@if (Model.GetMarkdownTemplate() == null)
{
    @Html.DisplayFor(m => m, "DocumentDefault")
}
else
{
    @Html.Raw((new MarkdownSharp.Markdown()).Transform(Model.GetMarkdownTemplate()(Model)))
}

