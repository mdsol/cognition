@model Cognition.Shared.Documents.Document
@if (Model.MarkdownTemplate == null)
{
    @Html.DisplayFor(m => m, "DocumentDefault")
}
else
{
    @Html.Raw((new MarkdownSharp.Markdown()).Transform(Model.MarkdownTemplate(Model)))
}

